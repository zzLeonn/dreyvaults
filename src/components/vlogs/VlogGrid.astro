---
import VlogCard from './VlogCard.astro';
import { getVlogs } from '../../utils/vlogData';

const vlogs = await getVlogs();
---

<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
  {vlogs.map(vlog => <VlogCard {vlog} />)}
</div>