---
import Layout from '../../layouts/Layout.astro';
import PageHeader from '../../components/common/PageHeader.astro';
import SearchBar from '../../components/common/SearchBar.astro';

---

<Layout title="Crochet Projects">
  <div class="pt-24 pb-12">
    <PageHeader
      title="Crochet Projects"
      description="Explore my handmade creations and crochet patterns"
    />
    
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex flex-col md:flex-row gap-8">
        <aside class="w-full md:w-64">
          <SearchBar />
          <div class="bg-white rounded-lg shadow-sm p-6 mt-6">
            <h2 class="font-serif text-xl text-gray-800 mb-4">Categories</h2>
            <ul class="space-y-2">
              {['Wearables', 'Home Decor', 'Accessories', 'Amigurumi', 'Blankets', 'Patterns'].map(category => (
                <li>
                  <a href={`/crochet/category/${category.toLowerCase()}`} class="text-gray-600 hover:text-primary transition-colors">
                    {category}
                  </a>
                </li>
              ))}
            </ul>
          </div>
        </aside>
        
        <main class="flex-1">
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {/* Project grid will go here */}
          </div>
        </main>
      </div>
    </div>
  </div>
</Layout>